# Fastlane / App Store Connect API key auth (recommended)
# Create a key in App Store Connect -> Users and Access -> Keys
# Keep the .p8 OUTSIDE the repo (example path below).

ASC_KEY_ID="YOUR_KEY_ID"
ASC_ISSUER_ID="YOUR_ISSUER_ID"
ASC_KEY_PATH="$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"

# Optional: wait for TestFlight processing (beta lane)
# WAIT_FOR_PROCESSING=1
# CRUMB_WAIT_FOR_PROCESSING=1

# Optional: use API key for deliver lanes (metadata/review submission)
# DELIVER_USE_API_KEY=1
