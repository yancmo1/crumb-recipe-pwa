# Example environment variables for Crumb
# Copy to .env and replace placeholders.

PORT=5554

# Local docker-compose DB password (change me)
CRUMB_POSTGRES_PASSWORD=change_me

# For containers (compose): use service name as host
DATABASE_URL=postgresql://crumb:change_me@crumb-postgres:5432/crumb

# For infra-new (Traefik) deployments:
CRUMB_HOST=crumb.yancmo.xyz
